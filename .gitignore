# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# uv
uv.lock

# Whisper model cache (downloaded on first run; can be GBs)
models/
.cache/

# Secrets — never commit
.env

# Local-only working notes / saved chats — never push to GitHub
tmp/

# macOS
.DS_Store
# AppleDouble sidecar files created on exFAT/non-HFS volumes
._*
